// frontend/src/config/app.config.js
// Single source for the app's display name - set VITE_APP_NAME in .env to
// rebrand every screen without touching component code.

export const APP_NAME = import.meta.env.VITE_APP_NAME || 'Screeno'
