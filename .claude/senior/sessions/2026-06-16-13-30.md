# Senior Analysis - 2026-06-16-13-30

## Session metadata
- Branch: dev
- Baseline commit: 565faf6
- HEAD commit: 4995e10 feat: persist tags and clean manager UI
- Files changed (HEAD~1): 60
- Files changed since baseline: 198
- Agents requested: SECURITY_AGENT, ARCHITECTURE_AGENT, DATABASE_EXPERT, REACT_EXPERT, UX_AGENT, BACKEND_EXPERT, PRODUCT_AGENT, CODE_QUALITY_AGENT, DEVOPS_AGENT, TEAMLEAD_AGENT, Senior Supabase/PostgreSQL Expert, Senior PDF/Document Expert
- Agent execution: six agents were attempted in parallel, but the platform returned usage-limit errors before usable reports were produced. The remaining roles were completed by local fallback review.

## What changed in this session
```text
 backend/migrations/001_initial_schema.sql          |    1 +
 backend/migrations/005_external_candidate_tags.sql |    4 +
 backend/refactor_repos.js                          |    9 +-
 backend/run-migration-005.js                       |   31 +
 backend/schema.json                                |    7 +
 backend/setup-db.js                                |    1 +
 backend/src/db/schema.js                           |    2 +-
 backend/src/models/external-candidates.model.js    |    1 +
 backend/src/repositories/external-candidate.repository.js | 5 +-
 backend/src/routes/client-template.routes.js       |   31 +-
 backend/src/routes/upload.routes.js                |   17 +-
 backend/src/services/email.service.js              |   19 +-
 backend/src/services/team.service.js               |    1 +
 backend/test/api-regression.js                     |    2 +-
 docs/AUDIT-AND-TESTING.md                          |    6 +-
 docs/database-schema.md                            |    7 +-
 docs/testing.md                                    |  481 ++------
 frontend/.env.example                              |    3 +
 frontend/src/components/manager/AddCandidateModal.jsx | 8 +-
 frontend/src/components/manager/ScheduleModal.jsx  |    7 +
 frontend/src/pages/auth/LoginPage.jsx              |   90 +-
 frontend/src/pages/manager/ClientInterviewsPage.jsx | 80 +-
 frontend/src/pages/manager/MonthlyAssessmentPage.jsx | 109 +-
 frontend/src/pages/manager/ResumeAnalyzerPage.jsx  |   58 +-
 frontend/src/pages/manager/TeamPage.jsx            |   37 +-
 frontend/src/styles/product-ui.css                 |   83 +-
 many unused prototype files/assets deleted
 60 files changed, 521 insertions(+), 10980 deletions(-)
```

## Previous suggestions implemented
- The P3 open issue to remove unused prototype pages is substantially implemented by deleting `frontend/src/screens`, `frontend/src/layouts`, `frontend/src/shared.jsx`, `frontend/src/data.jsx`, and the loose prototype `frontend/src/pages/v2-*` files.
- Demo login credentials were moved out of source and are now controlled by `VITE_SHOW_DEMO_ACCOUNTS` and `VITE_DEMO_ACCOUNTS`.
- External candidate tags now have schema, migration, repository, model, upload, and UI plumbing.
- Monthly calendar rows now group repeated records for the same internal candidate into one row with month chips.

---

## SECURITY_AGENT Report

### Critical (fix before next commit)
- None.

### High (fix this sprint)
- Raw JD text is interpolated directly into outbound HTML email | backend/src/services/email.service.js:370 | A manager-supplied JD can inject arbitrary HTML into candidate emails; escape `jdText` before rendering the email body.

### Medium (tech debt - schedule it)
- Demo account configuration is env-driven, but credentials still appear in the built client whenever enabled | frontend/src/pages/auth/LoginPage.jsx:17 | This is acceptable for local/staging demos, but production deployments should keep `VITE_SHOW_DEMO_ACCOUNTS=false` and document that these are public client-side values.

### What's good
- Client-template and external-candidate routes keep manager auth/role middleware in place.
- External candidate lookups use company ownership checks in repository/service paths.

### Top recommendation
Escape all user-controlled strings in email HTML, starting with JD text.

---

## ARCHITECTURE_AGENT Report

### Critical (fix before next commit)
- None.

### High (fix this sprint)
- External candidate tags are persisted but not integrated into client-mandate matching | backend/src/routes/client-template.routes.js:111, frontend/src/pages/manager/ClientInterviewsPage.jsx:520 | The new DB field is not used where mandates recommend and schedule candidates, so one major tag flow remains incomplete.

### Medium (tech debt - schedule it)
- `AddCandidateModal` has an external-candidate branch that bypasses the richer external-candidate flow | frontend/src/components/manager/AddCandidateModal.jsx:104 | There are now two creation surfaces with different capabilities, which will create inconsistent records.
- Documentation still describes deleted frontend prototype folders as present | CLAUDE.md:71, frontend/CLAUDE.md:61 | New contributors will be pointed at paths that no longer exist.

### What's good
- The tag storage change stays mostly in repository/service/route layers without introducing SQL in frontend or services.
- The main manager pages are moving toward shared product UI classes instead of one-off prototype screens.

### Top recommendation
Unify external candidate creation and mandate matching around the same candidate identity model.

---

## DATABASE_EXPERT Report

### Critical (fix before next commit)
- None.

### High (fix this sprint)
- No high-severity data bug found in the new migration.

### Medium (tech debt - schedule it)
- `external_candidates.tags` is a `TEXT` JSON string with no validation | backend/migrations/005_external_candidate_tags.sql:4 | This matches existing `users.tags`, but malformed values can silently become empty matches across the UI.
- No index accompanies the new tag/matching-related access pattern | backend/src/repositories/external-candidate.repository.js:39 | At minimum, ensure `external_candidates.company_id` and `interviews.external_candidate_id` are indexed before larger rosters.

### What's good
- Migration 005 is idempotent and included in `setup-db.js`.
- Repository writes serialize arrays consistently with the existing `tags` storage convention.

### Top recommendation
Add a small tag-normalization helper at repository/service boundaries so malformed JSON cannot leak into matching flows.

---

## REACT_EXPERT Report

### Critical (fix before next commit)
- None.

### High (fix this sprint)
- Bulk add selected members suppresses per-row API failures and still reports full success | frontend/src/components/manager/AddCandidateModal.jsx:76 | Managers can believe multiple users were added when some requests failed.

### Medium (tech debt - schedule it)
- Reports loading uses `.then().catch()` instead of project async/await style | frontend/src/pages/manager/ClientInterviewsPage.jsx:543 | This violates the local coding standard and makes error/loading paths less consistent.
- Hardcoded hex colors remain in touched login/add-candidate components | frontend/src/pages/auth/LoginPage.jsx:31, frontend/src/components/manager/AddCandidateModal.jsx:168 | This keeps UI polish dependent on ad hoc colors rather than tokens.

### What's good
- Monthly calendar grouping now uses a candidate-level map and stable month buckets instead of rendering duplicate candidate rows.
- Demo accounts are parsed from env and filtered for required fields.

### Top recommendation
Fix `AddCandidateModal` bulk-add error handling so partial failures are visible and retryable.

---

## UX_AGENT Report

### Critical (fix before next commit)
- None.

### High (fix this sprint)
- The older manual external option says "Add to Team" but creates an external candidate and refreshes only the internal list | frontend/src/components/manager/AddCandidateModal.jsx:122 | Users can add someone and then not see them in the current view, which looks like data loss.

### Medium (tech debt - schedule it)
- Client mandate candidates only show organization members, not external candidates | frontend/src/pages/manager/ClientInterviewsPage.jsx:701 | Managers who added external candidates for client work will not find them in the mandate assignment workspace.
- Calendar month chips are compact, but dense months may become hard to scan at 1180px min width | frontend/src/styles/product-ui.css:665 | Consider an overflow detail popover for multiple subjects in the same month.

### What's good
- The duplicated-candidate calendar screenshot case is addressed by grouping subjects into a single candidate row.
- The mandate and monthly pages now have clearer list-to-detail structure and better responsive page width.

### Top recommendation
Remove or route the confusing manual external path to the richer external-candidate modal.

---

## BACKEND_EXPERT Report

### Critical (fix before next commit)
- None.

### High (fix this sprint)
- Internal resume tag extraction remains fire-and-forget | backend/src/routes/upload.routes.js:33 | If the process restarts or extraction fails after response, the resume is saved but tags may never persist and the caller cannot know.

### Medium (tech debt - schedule it)
- External resume uploads use a timestamp storage key before the candidate row exists | backend/src/routes/upload.routes.js:51 | If candidate creation fails after upload, storage can contain an orphaned resume path.
- Client template patch cannot intentionally clear several fields because empty strings become null and `COALESCE` preserves old values | backend/src/repositories/client-template.repository.js:57 | Editing metadata to blank may appear to succeed without changing stored data.

### What's good
- External resume tag extraction is awaited before candidate creation through the TeamPage external modal path.
- Email sender naming no longer hardcodes the old company name.

### Top recommendation
Make resume tag extraction a tracked, awaited, or retryable operation instead of silent background work.

---

## PRODUCT_AGENT Report

### Critical (fix before next commit)
- None.

### High (fix this sprint)
- External candidate tags are now stored but external candidates are absent from mandate recommendations | backend/src/routes/client-template.routes.js:111, frontend/src/pages/manager/ClientInterviewsPage.jsx:753 | A manager using external candidates for client hiring will not see the benefit of the new tag work.

### Medium (tech debt - schedule it)
- The open-issues file still lists prototype removal as open after the cleanup commit removed those files | docs/open-issues.md:58 | Product tracking now understates completed work and will create duplicate cleanup requests.
- The login page still has a polished demo-account surface, but `.env.example` only shows an empty JSON array | frontend/.env.example:3 | Local setup would be smoother with a documented shape using placeholder values.

### What's good
- Static/mock prototype data was removed instead of kept hidden.
- The candidate calendar UX now reflects the user's requested "same candidate, same row, months selected" behavior.

### Top recommendation
Finish the external-candidate story in client mandates so stored tags have a visible product payoff.

---

## CODE_QUALITY_AGENT Report

### Critical (fix before next commit)
- None.

### High (fix this sprint)
- Silent `.catch(() => {})` in bulk add hides failed writes | frontend/src/components/manager/AddCandidateModal.jsx:84 | This will compound data-quality problems because the UI reports success without knowing what happened.

### Medium (tech debt - schedule it)
- Hardcoded hex colors remain in touched UI files | frontend/src/pages/auth/LoginPage.jsx:31, frontend/src/components/manager/AddCandidateModal.jsx:216 | This conflicts with the project's token rule.
- Several touched components remain large and inline-style heavy | frontend/src/pages/manager/TeamPage.jsx:105, frontend/src/pages/manager/ClientInterviewsPage.jsx:503 | Future UI fixes will be slower until these are split into smaller components.

### What's good
- Large unused prototype surface was removed, shrinking the codebase substantially.
- New tag parsing helpers are simple and defensive.

### Top recommendation
Replace silent catches with explicit partial-success handling and user-visible messages.

---

## DEVOPS_AGENT Report

### Critical (fix before next commit)
- None.

### High (fix this sprint)
- No deployment-blocking config issue found in the latest commit.

### Medium (tech debt - schedule it)
- `frontend/.env.example` does not show the expected demo-account object shape | frontend/.env.example:3 | Developers can enable demo accounts without knowing the required keys.
- The one-off migration runner exits the process directly | backend/run-migration-005.js:25 | It is okay as a script, but setup-db should remain the canonical deployment path.
- Frontend bundle-size warning remains an open deployment/performance issue | docs/open-issues.md:37 | The latest cleanup did not address route-level code splitting.

### What's good
- `setup-db.js` includes migration 005, so normal environment setup will apply the schema update.
- Demo credentials are no longer committed as source constants.

### Top recommendation
Document the exact local demo-account env JSON shape without committing real credentials.

---

## TEAMLEAD_AGENT Report

### Critical (fix before next commit)
- None.

### High (fix this sprint)
- Docs now contradict the cleaned file tree | CLAUDE.md:71, frontend/CLAUDE.md:61, docs/open-issues.md:58 | The code removed prototypes, but contributor docs still describe them as present/open.

### Medium (tech debt - schedule it)
- Commit message is consistent and descriptive, but the change bundles DB migration, UI cleanup, docs, and large deletions | git log | Future review would be easier if cleanup and behavior changes land separately.

### What's good
- The team paid down major dead-code debt instead of layering another UI pass on top of prototypes.
- The open issue about unused prototype pages is effectively completed.

### Top recommendation
Immediately update contributor docs and close the prototype-removal open issue.

---

## Senior Supabase/PostgreSQL Expert Report

### Critical (fix before next commit)
- None.

### High (fix this sprint)
- None.

### Medium (tech debt - schedule it)
- The migration adds nullable tag text but no data cleanup for existing external candidates | backend/migrations/005_external_candidate_tags.sql:4 | Existing external resumes will remain untagged unless reuploaded or backfilled.
- Tag matching is currently array-equality in application code, not database-assisted | backend/src/routes/client-template.routes.js:109 | This is fine for small teams but will not scale to large companies.

### What's good
- No foreign keys were introduced.
- Queries continue using the project `@param` style.

### Top recommendation
Plan a lightweight backfill path for existing external candidates with resumes.

---

## Senior PDF/Document Expert Report

### Critical (fix before next commit)
- None.

### High (fix this sprint)
- None.

### Medium (tech debt - schedule it)
- Internal resume extraction runs after the upload response and can be lost | backend/src/routes/upload.routes.js:33 | The user sees upload success even if tag extraction never persists.
- External upload accepts PDF/DOC/DOCX in the UI but not TXT in the external modal | frontend/src/pages/manager/TeamPage.jsx:92 | Backend allows text/plain, so the UI is inconsistent.
- Existing external candidates have no tag backfill | backend/migrations/005_external_candidate_tags.sql:4 | The new field only helps newly uploaded resumes.

### What's good
- Multer has a 5 MB limit and MIME filtering.
- External candidate resume upload now extracts tags before creating the candidate through the main external modal.

### Top recommendation
Make document-derived tags a durable job or an awaited operation with clear user feedback.

---

## Combined priority list

### All Critical items
- None.

### All High items
- [SECURITY] Raw JD text is interpolated directly into outbound HTML email | backend/src/services/email.service.js:370
- [ARCHITECTURE/PRODUCT] External candidate tags are persisted but not integrated into client-mandate matching | backend/src/routes/client-template.routes.js:111, frontend/src/pages/manager/ClientInterviewsPage.jsx:520
- [REACT/CODE_QUALITY] Bulk add selected members suppresses API failures and still reports success | frontend/src/components/manager/AddCandidateModal.jsx:76
- [UX] The older manual external option creates an external candidate from the internal add-member modal and refreshes only the internal list | frontend/src/components/manager/AddCandidateModal.jsx:104
- [BACKEND] Internal resume tag extraction remains fire-and-forget | backend/src/routes/upload.routes.js:33
- [TEAMLEAD] Docs now contradict the cleaned file tree | CLAUDE.md:71, frontend/CLAUDE.md:61, docs/open-issues.md:58

### Top 3 actions for next session
1. Include external candidates in client-mandate matching/scheduling or explicitly scope mandates to internal users only.
2. Escape JD text in `sendJDForResumeUpdate` email HTML.
3. Remove the duplicate/confusing external path from `AddCandidateModal` and fix bulk-add partial-failure handling.
