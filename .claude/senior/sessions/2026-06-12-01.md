# Senior Analysis — 2026-06-12-01

## Session metadata
- Branch: dev
- Baseline commit: 565faf6 (feat: normalize DB schema + wire new feature routes)
- HEAD commit: 565faf6 (same — no new commits since baseline)
- Files changed (HEAD~1): 80+ files across backend + frontend + docs
- Agents spawned: DATABASE_EXPERT, SUPABASE_EXPERT, BACKEND_EXPERT, SECURITY_AGENT, REACT_EXPERT, UX_AGENT, ARCHITECTURE_AGENT, CODE_QUALITY_AGENT, PRODUCT_AGENT, TEAMLEAD_AGENT, DEVOPS_AGENT

## What changed in this session (HEAD~1 summary)
- Fixed sc.overall_score → sc.overall AS overall_score in report.repository.js
- Fixed ScheduleModal voiceMode → interviewMode payload key
- Added field normalization in monthly-assessment.routes.js POST /
- Applied DB migrations: external_candidates, client_templates, monthly_assessments, monthly_assessment_enrollments
- Added migration 003 columns (availability, tags, interview FKs, report columns)
- Deleted old template.routes.js, team.routes.js
- Deleted docs/AUDIT-BACKLOG.md, docs/backend-prompt.md, docs/frontend-prompt.md
- Wired client-template.routes.js at /api/templates/client
- Wired monthly-assessment.routes.js at /api/assessments/monthly
- Updated BRAIN.md (311 line diff)

## Previous suggestions implemented
- sc.overall_score column bug → FIXED in report.repository.js
- interviewMode required validation → FIXED (both backend validation + frontend field name)
- monthly_assessments table missing → FIXED in setup-db.js + migrations

---

## DATABASE_EXPERT + SUPABASE_EXPERT Report

### Critical
- **team-member.repository.js SELECT_COLS reads `u.tags` and `u.availability` but migration 003 adds those to `team_members`, not `users`** | team-member.repository.js:13-14 | Every team member list call returns NULL for tags/availability.
- **monthly_assessment_enrollments has no `interview_id` column in CREATE TABLE but getCalendarByManager JOINs on e.interview_id** | setup-db.js:299-311, monthly-assessment.repository.js:70 | Calendar JOIN silently broken.
- **scorecards CREATE TABLE has `candidate_id INT NOT NULL` and `interviewer_id INT NOT NULL` without DEFAULT** | setup-db.js:126 | AI interview scorecard INSERTs will fail constraint violation.
- **team_members UNIQUE(manager_id, user_id) added but user_id can be NULL — ON CONFLICT clause in create() silently no-ops for NULL user_id rows**

### High
- getReportsByManager LEFT JOIN scorecards can produce duplicate rows if multiple scorecards per interview
- client-template.repository.js update() uses `data.field || null` — headcount: 0 is falsy, COALESCE keeps old value
- getByInterview @interviewId param uses camelCase while rest of file uses snake_case

### Medium
- topics/sub_topics/tags stored as TEXT not JSONB
- external_candidates has no manager_id column
- getHistoryByUser has no LIMIT

### What's good
- sc.overall AS overall_score fix is clean and correct
- COALESCE UPDATE pattern in client-template.repository.js is right for PATCH semantics

### Top recommendation
Fix team-member.repository.js SELECT_COLS to use tm.tags/tm.availability instead of u.tags/u.availability.

---

## BACKEND_EXPERT Report

### Critical
- **Raw SQL db.query() directly in profile.routes.js:51** — violates SQL-in-repositories rule
- **/send-jd fires N sequential getById calls with no company ownership check** | client-template.routes.js:116-129 | Manager can email users from other companies
- **extractTextFromBuffer defined inside route file** | upload.routes.js:14-34 | Business logic in HTTP layer
- **email.service.js getRecipients() doesn't enforce STATIC_RECIPIENTS** | email.service.js:48-60 | Real candidate emails delivered immediately if BREVO configured

### High
- finishScheduleSetup is fire-and-forget with no retry mechanism
- Enrollment loop sequential (20 members = 20 DB round-trips) | monthly-assessment.routes.js:28-34
- /generate-subtopics + /generate-jd have no input validation/length cap
- getEmailDeliveries and resendMagicLink don't check interview ownership
- importFromCSV is a stub returning zeros | team.service.js:99-102

### Medium
- Field normalization belongs in service layer not route
- JSON.parse(template.tags) in route handler belongs in service
- profile.routes.js uses .then().catch() anti-pattern
- upload.routes.js dynamic require() inside function body

### What's good
- interviewMode validation at service layer (throws early before any DB write)
- client-template.routes.js ownership pattern (getById with manager_id) consistently applied

### Top recommendation
Add company-scoped ownership check to userIds loop in /send-jd — authorization bypass must close before production.

---

## SECURITY_AGENT Report

### Critical
- **send-jd cross-company email exfiltration** | client-template.routes.js:117 | Manager can pass any user IDs and trigger emails to users from other companies
- **External candidate IDOR** | schedule.service.js:27 | externalCandidateRepository.getById has no company filter

### High
- Monthly assessment team_member_ids not ownership-checked | monthly-assessment.routes.js:28-34
- getEmailDeliveries companyId silently unused | schedule.service.js:113-117
- resendMagicLink companyId silently unused | schedule.service.js:119
- Uncaught JSON.parse on DB-stored tags data | client-template.routes.js:82,86

### Medium
- No array element type/length validation on userIds
- No array element validation on team_member_ids
- interviewMode not validated against allowlist
- Raw magic-link tokens stored and resent (documented deferral)

### What's good
- Template ownership consistently threaded via getById(id, req.user.id)
- teamMemberId ownership via getByIdForManager correctly applied

### Top recommendation
Add company_id filter to externalCandidateRepository.getById — one-line repo fix closes IDOR across all companies.

---

## REACT_EXPERT + UX_AGENT Report

### Critical
- **voiceMode state name vs interviewMode payload key** — maintainability trap | ScheduleModal.jsx:232,337
- **Step 3 validation passes with empty candidates array, dead-end on Step 4** | ScheduleModal.jsx:673,310
- **MonthlyAssessmentPage Step 2 no validation before advancing** — silent bad data to LLM | MonthlyAssessmentPage.jsx:178-179
- **ClientInterviewsPage handleSendJD swallows errors silently** | ClientInterviewsPage.jsx:145

### High
- ScheduleModal Step 4 always shows Mic icon regardless of stage type
- CandidateChips dropdown UX issues (doesn't close/reopen cleanly)
- MonthlyAssessmentPage Step 4 no guard against zero-selected-candidates submission
- CalendarData JSON.parse without try/catch — one bad row crashes tab | MonthlyAssessmentPage.jsx:217
- ScheduleModal useEffect uses .then().catch() | ScheduleModal.jsx:265-267

### Medium
- CandidateChips/EmailChips should be in shared/ not inline in ScheduleModal
- onMouseEnter/Leave inline style mutations (not CSS tokens/classes)
- WizardModal should be extracted from MonthlyAssessmentPage
- localStorage read for manager email repeated — needs useCurrentUser() hook

### What's good
- ScheduleModal correctly imports Avatar from shared/Avatar — local Avatar duplication resolved in this file
- ClientInterviewsPage now fully wired to real API calls with loading/error/empty states

### Top recommendation
Add validation guard in MonthlyAssessmentPage Step 4 to prevent submission with zero candidates.

---

## ARCHITECTURE_AGENT + CODE_QUALITY_AGENT Report

### Critical
- **getCalendarEvents signature mismatch** | schedule.routes.js:100 vs schedule.service.js:93 | Route calls `getCalendarEvents(req.user.companyId, req.query.week)` but service takes `(managerId)` — wrong ID passed, week ignored
- **Raw SQL in profile.routes.js:51** | Architecture violation: SQL belongs in repositories only

### High
- Field normalization in route handler (belongs in service) | monthly-assessment.routes.js:16-23
- Sequential await in enrollment loop | monthly-assessment.routes.js:28-34
- Sequential await in send-jd loop | client-template.routes.js:116-130
- N+1 getEnrollmentsByAssessment called per assessment | monthly-assessment.routes.js:47-49
- N+1 userRepository.getById per user in send-jd

### Medium
- Tag matching filter/sort in JS not SQL | client-template.routes.js:85-89
- .then().catch() chain | profile.routes.js:87-92
- getEmailDeliveries missing ownership check
- week query param silently ignored after getCalendarEvents
- Mount path drift: /api/templates/client vs /api/templates in docs

### What's good
- finishScheduleSetup correctly fire-and-forget (SMTP never blocks response)
- send-jd partial-failure handling accumulates sent/failed arrays — good resilience

### Top recommendation
Fix getCalendarEvents signature mismatch — silent wrong-user/empty calendar in production with no error.

---

## PRODUCT_AGENT + TEAMLEAD_AGENT Report

### Critical
- **BRAIN.md "Current Gaps" section stale** — still lists already-fixed bugs as open | BRAIN.md:114-128
- **/extract-tags route after /:id parametric routes** | client-template.routes.js:64 | Future GET /extract-tags would 404
- **getByInterview LIMIT 1 syntax** — LIMIT is PostgreSQL only; SQL Server uses FETCH FIRST | report.repository.js:27

### High
- BRAIN.md missing: getMonthlyAssessmentCalendar api export missing from api.js
- /extract-tags has no character limit — unbounded LLM cost
- Avatar component still duplicated in 6 files
- AUDIT-BACKLOG.md deleted without confirming all items ported to open-issues.md

### Medium
- report.repository.js getByInterview uses bare SELECT *
- client-template COALESCE prevents intentional null-clears
- monthly-assessment enrollment N+1 insert loop
- Year calendar UI may be empty — no confirmed wired API call

### What's good
- sc.overall alias fix is clean and high-visibility
- New route files follow 3-layer architecture correctly

### Top recommendation
Update BRAIN.md Current Gaps immediately — stale docs cause next session to re-investigate already-solved problems.

---

## DEVOPS_AGENT Report

### Critical
- No SIGTERM handler — reportJobWorker hard-killed on deploy, jobs stuck in status='started'
- Hardcoded test credentials seeded on every setup-db.js run | setup-db.js:341-355
- unhandledRejection/uncaughtException handlers swallow errors silently

### High
- /health has no DB liveness check
- setup-db.js step 12 DELETE FROM candidates runs on every re-run (destructive DML)
- Backfill INSERT in step 8a runs on every re-run (unnecessary lock-heavy scan)
- No startup validation of required env vars

### Medium
- setup-db.js should split into migrate.js (DDL) + seed.js (data)
- unhandledRejection doesn't exit process
- writeLimiter may be too tight for exam answer submissions
- No structured logging
- mssql installs even when DB_TYPE=supabase

### What's good
- CORS origin allowlist reads FRONTEND_URL as comma-split list (multi-origin staging support)
- All DDL uses IF NOT EXISTS guards — setup-db.js is genuinely re-runnable

### Top recommendation
Add SIGTERM handler to stop report-job worker and drain connections before exiting.

---

## Combined priority list

### All Critical items
- [DATABASE] team-member.repository.js uses u.tags/u.availability — wrong table, should be tm.*
- [DATABASE] monthly_assessment_enrollments missing interview_id column in DDL
- [DATABASE] scorecards NOT NULL columns (candidate_id, interviewer_id) break AI interview scorecard inserts
- [BACKEND/SECURITY] send-jd fires N userRepository.getById with no company ownership — email exfiltration
- [BACKEND] Raw SQL db.query() in profile.routes.js:51 — layer violation
- [SECURITY] External candidate IDOR — no company_id filter on externalCandidateRepository.getById
- [ARCHITECTURE] getCalendarEvents(companyId, week) vs service signature (managerId) — wrong data returned
- [PRODUCT] BRAIN.md Current Gaps stale — lists fixed bugs as open

### All High items
- [BACKEND] Enrollment loop sequential (Promise.all needed)
- [BACKEND] send-jd loop sequential + N+1 user fetches
- [SECURITY] team_member_ids not ownership-checked in assessment enrollment
- [SECURITY] getEmailDeliveries/resendMagicLink companyId unused
- [SECURITY] JSON.parse on stored tags without try/catch
- [REACT] MonthlyAssessmentPage Step 4 zero-candidate guard missing
- [REACT] CalendarData JSON.parse without try/catch
- [REACT] ScheduleModal useEffect uses .then().catch()
- [PRODUCT] api.js missing getMonthlyAssessmentCalendar export
- [DEVOPS] /health has no DB liveness check

### Top 3 actions for next session
1. Fix team-member.repository.js SELECT_COLS (u.tags → tm.tags, u.availability → tm.availability) — every manager dashboard call returns wrong data
2. Fix getCalendarEvents signature mismatch in schedule.routes.js — silent wrong-user calendar
3. Fix send-jd /api/templates/client/:id/send-jd ownership check on userIds — security issue
